# TC-002: 鸿蒙官方模板测试

**测试目标**：验证 Planner 能正确引用鸿蒙官方模板生成项目骨架

---

## 提示词（发送给 Planner）

```
你是编码项目架构师。请为以下需求创建开发计划：

需求：做一个鸿蒙记事本应用

技术要求：
- 模板：鸿蒙官方
- 版本号：v0.0.1

请创建项目骨架、dev-plan.md、design.md、feature-spec.md。
```

---

## 预期结果

**项目结构**：
```
{project}/
├── docs/
│   ├── dev-plan.md          # 应包含 "使用模板：鸿蒙官方"
│   ├── design.md
│   └── feature-spec.md
├── AppScope/                ✅ 鸿蒙应用全局配置
│   └── app.json5
├── entry/                   ✅ 主模块
│   ├── src/main/ets/
│   │   ├── entryability/
│   │   └── pages/
│   └── build-profile.json5
├── build-profile.json5      ✅ 项目构建配置
├── oh-package.json5         ✅ 依赖配置
└── hvigorfile.ts            ✅ 构建脚本
```

**dev-plan.md 检查点**：
- [ ] "使用模板：鸿蒙官方" 字段存在
- [ ] 项目类型：纯前端（鸿蒙应用）
- [ ] 任务列表包含项目骨架任务

**目录检查**：
- [ ] AppScope/ 存在
- [ ] entry/ 存在
- [ ] build-profile.json5 存在
- [ ] oh-package.json5 存在

---

## 验证命令

```bash
# 检查目录结构
ls -la {project}/
ls -la {project}/AppScope/
ls -la {project}/entry/

# 检查 dev-plan.md 模板字段
grep "使用模板" {project}/docs/dev-plan.md

# 检查鸿蒙配置文件
cat {project}/build-profile.json5
cat {project}/oh-package.json5
```
