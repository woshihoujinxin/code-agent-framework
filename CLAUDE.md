# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

## Overview

This is a **multi-agent autonomous development framework** (无人值守多智能体编码框架). It orchestrates software development through specialized agents that collaborate via file-based communication, implementing a three-layer loop: outer (continuous requirements), middle (batch development), and inner (bug fixes).

**Core architecture:** Requirements → PM → Planner (with test contracts) → Frontend/Backend Devs (unit tests + self-check) → 5-Dimensional Testing (correctness/quality/robustness/security/E2E) → Fix Loop → code-sage rule extraction.

## Available Commands

| Command | Purpose | Flow |
|---------|---------|------|
| `/goal-d <requirement>` | R&D quality orchestration → high-quality code | PM → Planner → Dev×2 → 5-Dim Tester → Fix Loop |
| `/goal-o <requirement>` | Delivery orchestration → deployable artifacts | Planner → Dev×2 → Reviewer → Builder → Validator |
| `/goal-init` | Deploy compatibility layer to project root | Copies `.opencode/` and `.reasonix/` to root |
| `/goal-tl [TASK_ID]` | View task list | Shows `docs/dev-plan.md` task table (with detail if TASK_ID provided) |
| `/goal-tr [TASK_ID]` | View 5-dimensional test results | Shows `tests/reports/results.json` summary (with detail if TASK_ID provided) |
| `/goal-resume [继续]` | Resume from breakpoint | Scans `docs/main-log.md` CHECKPOINT + `docs/dev-plan.md` states, reports save point (empty arg) or resumes precisely per task state (`继续`) |
| `/goal-r <repo_urls>` | Research sub-pipeline (complex needs) | Clone repos → code-researcher → technical docs → auto-continue to `/goal-d` |

> Use `/goal-d` for development with 5-dimensional quality gates (functionality/quality/robustness/security/E2E). Use `/goal-o` for delivery with review→build→validation pipeline.

## Project Structure

| Directory | Purpose | Maintained by |
|-----------|---------|---------------|
| `agents/` | 19 subagent definitions (PM/Planner/Dev/Tester/Ops/etc.) | **Source of truth**, edit directly |
| `commands/` | Slash commands (`/goal-d`, `/goal-o`, etc.) | **Source of truth**, edit directly |
| `skills/` | Knowledge base (coding standards, design systems, prototype templates, grill-me) | **Source of truth**, edit directly |
| `orchestrators/` | Orchestrator definitions (dev-quality, delivery, demo) | **Source of truth**, edit directly |
| `.opencode/` | opencode compatibility layer (generated, **do not edit**) | `tools/sync-compat.py build` |
| `.reasonix/` | Reasonix compatibility layer (generated, **do not edit**) | `tools/sync-compat.py build` |
| `tools/sync-compat.py` | Compatibility layer generator | Edit generation logic only |
| `money/.claude/` | Legacy/mirror directory (ignore) | — |

**Never hand-edit** `.opencode/` or `.reasonix/` — they are thin pointer shells generated by `sync-compat.py`. All content lives in `agents/`, `commands/`, and `skills/`.

## Key Architecture Concepts

### Three-Layer Loop

1. **Outer layer** (continuous): Ingests requirements, never exits
2. **Middle layer ①** (development): Full-batch parallel dev → smoke check → mark 🔳 (pending test)
3. **Middle layer ②** (testing): Version-level 5-dimensional testing → fix loop (≤3 rounds)
4. **Inner layer** (self-evolution): code-sage extracts rules → coding-standards →下次迭代

### Test Contracts (F/B/S/E/Q)

The **test contract** in `docs/feature-spec.md` is the **shared baseline** for Dev unit tests and Tester verification — a hard contract that cannot be violated:

- **F (Functionality)**: Business use cases, Dev must cover with unit tests
- **B (Robustness)**: Boundary/edge cases, Dev must cover with unit tests
- **S (Security)**: Attack scenarios, Dev must cover with unit tests
- **E (E2E)**: End-to-end flows, verified by E2E tester
- **Q (Quality)**: Quality checklist (non-test format)

Each use case must include: US source, role (FE/BE/both), input, expected output. Dev self-check reports must declare "covered F/B/S case numbers". Tester reports must include "verification matrix by case".

### Worktree Testing

Tests run in a **version-level worktree** (`tests/ws-{version}`), not the main repository. This isolates testing from concurrent development. The worktree is checked out to the `feature/{version}` branch and synced before each test.

### DAG-Based Task Scheduling

Tasks are scheduled based on dependency graph (DAG) from `docs/dev-plan.md`:
- Tasks with satisfied dependencies form the **ready set** (`ready_dev`)
- All ready tasks are developed in parallel (default: maximize concurrency)
- After all ⏳ become 🔳, **Phase 3 testing begins** for the entire version

### Version Branching

All development and bug fixes commit to a **version branch** `feature/{version}` (version from `git tag`, e.g., v0.0.1). Testing is based on this branch (logical complete version). After all tests pass:
- Merge to main: `git checkout main && git merge feature/{version}`
- Tag: `git tag v{version}`
- Cleanup: Remove worktree and branch

## Agent Types

| Agent | File | Purpose |
|-------|------|---------|
| PM | `agents/code-product-manager.md` | Analyzes requirements → `docs/prd.md` |
| Planner | `agents/code-planner.md` | Splits tasks → `dev-plan.md` + `feature-spec.md` + project skeleton |
| Frontend Dev | `agents/code-dev-frontend.md` | Implements frontend + unit tests + self-check |
| Backend Dev | `agents/code-dev-backend.md` | Implements backend + unit tests + self-check |
| Ops | `agents/code-ops.md` | Prepares test environment (dependencies, DB, ports) |
| Correctness Tester | `agents/code-tester-correctness.md` | Validates against F use cases |
| Quality Tester | `agents/code-tester-quality.md` | Code quality review |
| Robustness Tester | `agents/code-tester-robustness.md` | Validates against B use cases |
| Security Tester | `agents/code-tester-security.md` | Validates against S use cases |
| E2E Tester | `agents/code-tester-e2e.md` | Validates against E use cases |
| Reviewer | `agents/code-reviewer.md` | Code review (delivery flow) |
| Builder | `agents/build-builder.md` | Build artifacts (delivery flow) |
| Validator | `agents/artifact-validator.md` | Validate artifacts (delivery flow) |
| code-sage | `agents/code-sage.md` | Extracts rules from failures → self-evolution |
| Prototype Builder | `agents/code-prototype-builder.md` | Builds HTML/CLI prototypes from PRD |
| Prototype Critic | `agents/code-prototype-critic.md` | Independent prototype review |
| Export Specialist | `agents/code-export-specialist.md` | Exports prototypes to HTML/PDF/PPTX/ZIP |
| Researcher | `agents/code-researcher.md` | Researches open-source repos → technical docs |

## Key Files (Runtime)

| File | Purpose | Written by |
|------|---------|------------|
| `docs/prd.md` | Product requirements (requirement pool) | PM |
| `docs/dev-plan.md` | Task status tracking (DAG, emoji states) | Planner (create), Orchestrator (update) |
| `docs/design.md` | Architecture design (ADR, diagrams, entities) | Planner |
| `docs/feature-spec.md` | Per-task specs + **test contracts (F/B/S/E/Q)** | Planner |
| `docs/lessons-learned.md` | Cross-task experience accumulation | Dev (code-level), Planner (architecture-level) |
| `docs/main-log.md` | Orchestrator's全过程档案 (checkpoint, events) | Orchestrator only |
| `docs/smoke-checks.md` | Smoke check commands (per task) | Planner/Dev |
| `docs/env-state.md` | Test environment state (dependencies, DB schema) | Ops |
| `tests/reports/results.json` | All test results (machine truth source) | Orchestrator aggregates from Tester JSON outputs |
| `tests/reports/{TASK_ID}-{dimension}.json` | Per-task per-dimension structured verdict | Testers (per `coding-standards/references/report-schema.md`) |
| `tests/reports/{TASK_ID}-selfcheck-{fe,be}.md` | Dev self-check reports | Devs |
| `tests/unit/test_{TASK_ID}_*.{ext}` | Unit tests (must cover F/B/S) | Devs |
| `docs/prototype/` | High-fidelity prototypes + DESIGN.md | Prototype Builder |
| `exports/` | Exported deliverables | Export Specialist |

**Task state emojis**: ⏳ pending | 🔄 in progress | 🔳 pending test | ✅ complete | ⚠️ low-quality pass

## Development Modes

| Mode | Trigger | Characteristics |
|------|---------|-----------------|
| **Standard SOP** | Multi-module/multi-end/complex state | Full process with 5-dimensional quality gates |
| **Quick Mode** | ≤10 source files/single-module/no complex state | Compressed loop (≤3 tasks, 1 test round, ≤2 fix rounds), **test contracts still produced** |
| **BugFix** | Explicit bug description | Resume relevant Dev + retest affected dimensions only, skip PM/Planner |
| **Stock Mode** (存量模式) | Existing project with source + git history | **Follow existing patterns** (architecture/testing/naming), don't force DDD/5-dim/full design docs; use project's own test commands for verification |

## DDD Methodology (Standard SOP + Complex Business Rules)

When injected `方法论：DDD` by the orchestrator (standard SOP + complex business rules), Planner extends to **domain modeling**:
- **Bounded contexts**: Divide business boundaries, write to design.md
- **Aggregates**: Identify aggregates/entities/value objects
- **Repository interfaces**: Per aggregate, signature-level definition
- **Directory skeleton**: `src/` with four-layer DDD structure (domain/application/interface/infrastructure)

Quick Mode/BugFix do NOT inject DDD.

## Build/Deploy Commands

```bash
# Generate compatibility layers (after editing agents/commands/skills)
python .claude/tools/sync-compat.py build

# Deploy to project root (copies .opencode/ and .reasonix/ to root)
python .claude/tools/sync-compat.py deploy .
# Or: /goal-init (Claude Code / Reasonix native; opencode must use terminal command)

# Update framework
cd .claude && git pull
```

## Important Constraints

- **UTF-8 hard constraint**: All logs/docs must be UTF-8. Never use shell `echo/printf >>` to append Chinese lines (will corrupt on Windows GBK). Use Write/Edit tools only.
- **Git commit mandatory**: Dev must commit to `feature/{version}` branch before testing. Tester reports must include commit hash.
- **IS_PASS mandatory**: Dev self-check reports must include `IS_PASS: YES/NO` line.
- **No direct code editing by orchestrator**: Orchestrator never edits code directly — all delegated to Dev agents.
- **Test contract immutability**: Contracts (F/B/S/E/Q) can only be changed by Planner, not Dev/Tester. If needed, route through Planner.
- **Concurrent control**: Two-layer concurrency — task-level (BATCH_SIZE) for development, dimension-level (MAX_PARALLEL, default 3) for testing. Peaks at max(2×|ready|, MAX_PARALLEL), never multiply.

## Three-Tool Compatibility

The same repository serves three tools with **single source of truth**:

| Capability | Claude Code | opencode | Reasonix |
|------------|-------------|----------|----------|
| 19 subagents | `agents/` (native) | `.opencode/agents/` (generated) | `.reasonix/skills/` (generated, runAs: subagent) |
| Commands | `commands/` (native) | `.opencode/commands/` (generated) | Native reads `.claude/commands`, zero changes |
| Knowledge base skills | `skills/` (native) | Native reads `.claude/skills`, zero changes | Native reads `.claude/skills`, zero changes |
| Project instructions | CLAUDE.md | AGENTS.md (fallback to CLAUDE.md) | REASONIX.md / AGENTS.md / CLAUDE.md |

opencode cannot use `/goal-init` for initialization (only reads `.opencode/commands/`, not `.claude/commands/`). Use terminal command: `python .claude/tools/sync-compat.py deploy .`

## Further Reading

- `README.md` — Quick start and directory conventions
- `orchestrators/README.md` — Complete flow diagrams, three-layer loops, role responsibilities, test contract mechanism, self-evolution loop
- `orchestrators/dev-quality-orchestrator.md` — Full orchestrator system prompt (Phase 0-4 details)
- `orchestrators/demo-todo-cli.md` — Complete demo script for Todo CLI development
